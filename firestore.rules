rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // กฎเดียว: ต้องล็อกอิน (Firebase Authentication) ก่อนจึงจะอ่าน/เขียนได้ทุก collection
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
