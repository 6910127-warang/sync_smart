# SmartSync Design System

**สถานะ:** ร่างเริ่มต้น (Draft v1) — เอกสารนี้เป็น design system อ้างอิงสำหรับงานออกแบบ UI/UX ในอนาคต **ยังไม่ผูกกับการเลือก framework หรือการเขียนโค้ดจริง** (ดูเฟสปัจจุบันของโครงการใน [CLAUDE.md](CLAUDE.md)) — ค่าต่างๆ ในนี้ (สี/ฟอนต์/spacing) เป็นข้อเสนอเชิงออกแบบที่พร้อมให้ทีมพัฒนานำไปใช้ต่อเมื่อเข้าเฟสพัฒนา ปรับแก้ได้ตามความเหมาะสม
**วันที่สร้าง:** 20260816

> เอกสารนี้ตอบโจทย์ **โทนภาพลักษณ์: Healthcare + Minimalist + Muji** — เน้นความสงบ น่าเชื่อถือ อ่านง่าย ลดสิ่งรบกวนสายตา เหมาะกับผู้ใช้งานที่เป็นบุคลากรสาธารณสุข (ไม่ใช่นักออกแบบ) ที่ต้องใช้งานจอคอมพิวเตอร์เป็นเวลานานในแต่ละวัน

## หมายเหตุสำคัญก่อนอ่าน (Assumptions)

- **โลโก้ต้นฉบับ** ([assets/brand/logo.png](assets/brand/logo.png)) ที่ผู้ใช้ให้มาเป็น **ภาพร่างแนวคิด (concept sketch)** แสดงโหนดเครือข่าย (ยา, ความปลอดภัย/RBAC, รายงาน/Dashboard, บุคลากร, จุดกระจายยา) เชื่อมโยงกันคล้ายเครือข่าย รพ.สต. — ยังไม่ใช่ไฟล์ vector สมบูรณ์ และใช้สีสันสดจัด (candy color) ซึ่ง**ไม่ตรงกับโทน muji ที่ต้องการโดยตรง**
- เอกสารนี้จึงตีความ **แนวคิด/โครงสร้าง** ของโลโก้ (โหนด + การเชื่อมโยงเป็นเครือข่าย) มาใช้เป็นภาษาภาพ (visual language) ของแบรนด์ แต่ **ปรับชุดสีให้ลดความอิ่มตัวลง (desaturate) ให้เข้ากับโทน healthcare/minimalist/muji** แทนการใช้สีจากภาพร่างตรงๆ
- **ควรทำให้เป็น vector logo ที่สมบูรณ์โดยนักออกแบบกราฟิกก่อนใช้งานจริง** — ไฟล์ปัจจุบันใช้อ้างอิงแนวคิดในเอกสารนี้เท่านั้น
- ค่าสี/ฟอนต์/spacing ในเอกสารนี้เป็น**ข้อเสนอเชิงออกแบบ**ของ Claude ไม่ใช่ข้อมูลทางคลินิกหรือนโยบาย — ผู้ใช้ (เจ้าของข้อกำหนด) ปรับแก้ได้ตามต้องการ

---

## 1. Brand Identity & CI

### 1.1 แนวคิดแบรนด์ (Brand Concept)

SmartSync คือระบบที่ทำให้ **เครือข่าย รพ.สต. 29 แห่งกับ รพ.แม่ข่าย เห็นข้อมูลชุดเดียวกันแบบเรียลไทม์** — แนวคิดโลโก้ที่ให้มาจึงสื่อถึง "โหนดที่เชื่อมต่อกัน" ได้ตรงกับแก่นของระบบอยู่แล้ว (ดูที่มาปัญหาใน [spec 001](01-requirements/01-spec/20260816-001-project-scope-and-problem-background.md)) องค์ประกอบหลักที่ควรคงไว้จากภาพร่าง:

- **โหนด (Node):** กล่องมุมโค้ง (rounded square) บรรจุไอคอนหนึ่งความหมาย — ใช้แทนหน่วยงาน/โมดูลของระบบ
- **เส้นเชื่อม (Connection line):** เส้นบางสีเทาอ่อน เชื่อมโหนดเข้าด้วยกัน — สื่อถึงการซิงค์ข้อมูลกลาง ไม่ใช่การกระจายอำนาจแบบไร้ศูนย์
- **โหนดกลาง (Central hub):** โหนดที่มีเส้นเชื่อมออกมากที่สุด — แทน รพ.แม่ข่าย/ระบบกลาง

### 1.2 ค่านิยมแบรนด์ (Brand Values)

| ค่านิยม | ความหมายต่อการออกแบบ |
|---|---|
| **น่าเชื่อถือ (Trustworthy)** | สีสงบ ไม่ฉูดฉาด, ใช้สีแดง/ส้มเข้มเฉพาะกรณีแจ้งเตือนจริงเท่านั้น (ยาใกล้หมด/เบิกฉุกเฉิน) เพื่อไม่ให้ผู้ใช้ตื่นตระหนกเกินจำเป็นหรือ "ชิน" กับสีเตือนจนมองข้าม |
| **เรียบง่าย ไม่รบกวนสายตา (Minimalist / Muji)** | พื้นที่ว่างเยอะ, ใช้สีน้อยสี, ไม่มีลวดลาย/เงา/gradient ที่ไม่จำเป็น, ให้ข้อมูล (ตัวเลขคงคลัง, สถานะคำขอ) เป็นพระเอกของหน้าจอ ไม่ใช่ตัว UI |
| **ใช้งานได้กับผู้ใช้หลากหลายระดับ (Inclusive)** | เจ้าหน้าที่ รพ.สต., เภสัชกร, ผู้บริหาร, แอดมิน มีพื้นฐานการใช้คอมพิวเตอร์ต่างกัน — ตัวอักษรใหญ่พออ่านสบาย, ปุ่มสำคัญชัดเจน, คำอธิบายไม่ใช้ศัพท์เทคนิคเกินจำเป็น |
| **เป็นเครือข่ายเดียวกัน (Connected)** | ทุกหน้าจอ (ของทุก role) ใช้ภาษาภาพชุดเดียวกัน แม้สิทธิ์การมองเห็นข้อมูลจะต่างกันตาม RBAC |

### 1.3 การใช้โลโก้ (Logo Usage)

- **Clear space:** เว้นระยะรอบโลโก้อย่างน้อยเท่าความสูงของโหนดกลาง 1 โหนด ไม่ให้มีองค์ประกอบอื่นมาชิด
- **พื้นหลังที่ใช้ได้:** พื้นขาว/พื้นสีกระดาษ (`--color-bg-paper`) เท่านั้นในเวอร์ชันแรก — ยังไม่กำหนดเวอร์ชัน reverse (พื้นเข้ม) จนกว่าจะมี dark mode ในสโคป (ปัจจุบันไม่อยู่ในขอบเขต)
- **ข้อห้าม:** ห้ามยืด/บีบสัดส่วน, ห้ามใส่เงา (drop shadow) เพิ่ม, ห้ามเปลี่ยนสีโหนดเดี่ยวๆ นอกชุดสีที่กำหนดในเอกสารนี้
- ไฟล์อ้างอิง: [assets/brand/logo.png](assets/brand/logo.png) *(ภาพร่างแนวคิด รอ vectorize)*

---

## 2. Design Tokens / Design System

### 2.1 Colors

โทนสีหลักคือ **โทนกระดาษ/ธรรมชาติแบบ Muji** (ครีม/เบจ/เทาอุ่น) เป็นพื้นหลังส่วนใหญ่ของหน้าจอ ใช้สีจากภาพร่างโลโก้เพียง 1 สีเป็น **Primary** (เขียวอมฟ้า/sage — จากโหนดกลางและโหนดล็อก) ที่ลดความอิ่มตัวลงให้เข้ากับโทน healthcare ส่วนสีอื่นในภาพร่าง (ม่วง/แดง/เหลือง/น้ำเงิน) ดึงมาเป็น **Semantic colors** (success/warning/danger/info) เท่านั้น ไม่ใช้เป็นสีตกแต่งทั่วไป — เพื่อรักษาความ minimalist

#### Neutral / Base (Muji paper tones)

| Token | Hex | การใช้งาน |
|---|---|---|
| `--color-bg-paper` | `#F7F5F1` | พื้นหลังหลักของแอป (แทนสีขาวล้วน) |
| `--color-bg-surface` | `#FFFFFF` | พื้นผิว card/table/modal ที่ต้องการแยกจากพื้นหลัง |
| `--color-bg-muted` | `#EFEAE2` | พื้นหลังแถบรอง, sidebar, แถวสลับสีตาราง (zebra row) |
| `--color-border` | `#DCD5C9` | เส้นขอบ, เส้นแบ่งตาราง |
| `--color-text-primary` | `#2E2B26` | ตัวอักษรหลัก (ink อุ่น ไม่ใช่ดำสนิท) |
| `--color-text-secondary` | `#6B6459` | label, helper text, timestamp |
| `--color-text-disabled` | `#A9A297` | ตัวอักษร/ไอคอนที่ถูกปิดใช้งาน |

#### Primary (จากโหนดกลาง/ล็อกในโลโก้ — ลดความอิ่มตัว)

| Token | Hex | การใช้งาน |
|---|---|---|
| `--color-primary-700` | `#2F5D53` | ปุ่มหลัก hover/active, ข้อความลิงก์เข้ม |
| `--color-primary-600` | `#3D7568` | ปุ่มหลัก (Primary Button), ไอคอน active, แถบนำทางที่เลือกอยู่ |
| `--color-primary-100` | `#DCEAE5` | พื้นหลังปุ่ม/badge แบบ subtle, สถานะ hover ของแถวตาราง |

#### Semantic (สถานะคำขอเบิก/แจ้งเตือน — ใช้อย่างจำกัด)

| Token | Hex | การใช้งาน |
|---|---|---|
| `--color-success` | `#4C7A5E` | สถานะ "รับยาแล้ว/จ่ายแล้ว", ยืนยันสำเร็จ |
| `--color-warning` | `#B8863E` | สถานะ "ใกล้ถึงรอบเบิก", ยอดคงคลังใกล้ safety stock |
| `--color-danger` | `#B14B3F` | สถานะ "ยาขาด/เบิกฉุกเฉิน", ปุ่ม/action ที่ทำลาย/ปฏิเสธคำขอ |
| `--color-info` | `#4A6FA5` | ข้อความแจ้งเตือนทั่วไป, สถานะ "รออนุมัติ" |

> **กฎการใช้สี semantic:** ใช้เฉพาะกับ badge สถานะ, ไอคอนแจ้งเตือน และปุ่ม action ที่ตรงความหมายเท่านั้น **ห้ามใช้เป็นสีพื้นหลังเต็มบล็อกขนาดใหญ่** (เช่น แถบสีแดงเต็มหน้าจอ) เพื่อคงความ minimalist และไม่ให้ผู้ใช้เครียดกับสีเตือนที่ขึ้นบ่อย (เบิกฉุกเฉินเกิดเกือบทุกเดือนตาม [spec 001](01-requirements/01-spec/20260816-001-project-scope-and-problem-background.md) — ถ้าสีเตือนแรงเกินไปจะกลายเป็น "noise" ที่ผู้ใช้ชินจนไม่สนใจ)

**Contrast:** ทุกคู่สีตัวอักษร/พื้นหลังข้างต้นผ่านเกณฑ์ WCAG AA (contrast ratio ≥ 4.5:1 สำหรับตัวอักษรขนาดปกติ) — ตรวจซ้ำอีกครั้งเมื่อมีการปรับ hex จริงตอน implement

### 2.2 Typography

ผู้ใช้งานหลักอ่านภาษาไทยเป็นหลัก ต้องเลือกฟอนต์ที่ **รองรับภาษาไทยได้ดี อ่านสบายบนจอ ไม่ใช่ฟอนต์ตกแต่ง**

| บริบทการใช้งาน | ฟอนต์ที่แนะนำ | เหตุผล |
|---|---|---|
| หน้าจอแอป (UI) — ไทย | **IBM Plex Sans Thai** (หรือ Noto Sans Thai เป็นตัวสำรอง) | humanist sans, เรียบ, น้ำหนักตัวอักษรสม่ำเสมอ เข้ากับโทน minimalist/muji มากกว่าฟอนต์ราชการที่บางเกินไปเมื่อแสดงบนจอ |
| หน้าจอแอป (UI) — อังกฤษ/ตัวเลข | **IBM Plex Sans** | จับคู่ family เดียวกับฟอนต์ไทยได้พอดี ตัวเลขอ่านง่าย (สำคัญมากเพราะเป็นระบบตัวเลขคงคลัง/จำนวนเบิก) |
| เอกสาร/รายงานที่พิมพ์หรือ export (เช่น ใบเบิก, ไฟล์ส่งออกไป INVC — [spec 004](01-requirements/01-spec/20260816-004-management-dashboard.md), [spec 005](01-requirements/01-spec/20260816-005-legacy-system-integration.md)) | **TH Sarabun New** | ฟอนต์มาตรฐานเอกสารราชการไทยที่หน่วยงานสาธารณสุขคุ้นเคย เหมาะกับเอกสารพิมพ์มากกว่าหน้าจอ |

**Type scale** (base 16px, ratio ~1.25 — เพื่อความชัดเจนบนจอที่ผู้ใช้อาจนั่งไกลกว่าเดิม):

| Token | Size / Line-height | การใช้งาน |
|---|---|---|
| `--font-size-display` | 32px / 40px, Medium | หัวข้อหน้า Dashboard ผู้บริหาร |
| `--font-size-h1` | 24px / 32px, Medium | หัวข้อหน้าหลัก |
| `--font-size-h2` | 20px / 28px, Medium | หัวข้อ section/card |
| `--font-size-body` | 16px / 24px, Regular | เนื้อหาทั่วไป, label ฟอร์ม, ตัวเลขในตาราง |
| `--font-size-small` | 14px / 20px, Regular | helper text, timestamp, หมายเหตุ |
| `--font-size-caption` | 12px / 16px, Regular | ป้ายกำกับรอง, footnote |

**กฎการใช้ตัวหนา:** ใช้ Medium/Semibold เฉพาะหัวข้อและตัวเลขสำคัญ (ยอดคงคลัง, ยอดแนะนำเบิก) — เนื้อหาทั่วไปใช้ Regular เสมอ เพื่อลดความ "แน่น" ของหน้าจอ ตรงกับหลัก muji ที่เลี่ยงน้ำหนักตัวอักษรหนักเกินจำเป็น

### 2.3 Spacing & Layout

ใช้ระบบ spacing แบบ 4px grid (คูณด้วย 4 ทั้งหมด เพื่อความสม่ำเสมอและง่ายต่อการคำนวณ):

| Token | Value | การใช้งานทั่วไป |
|---|---|---|
| `--space-1` | 4px | ระยะห่างระหว่างไอคอนกับ label ติดกัน |
| `--space-2` | 8px | ระยะภายในปุ่มเล็ก, gap ระหว่าง field ในฟอร์มเดียวกัน |
| `--space-3` | 12px | padding ภายใน input/บรรทัดตาราง |
| `--space-4` | 16px | padding ภายใน card, ระยะห่างระหว่างองค์ประกอบทั่วไป |
| `--space-6` | 24px | ระยะห่างระหว่าง section ย่อยในหน้าเดียวกัน |
| `--space-8` | 32px | ระยะห่างระหว่าง section หลัก |
| `--space-12` | 48px | ระยะขอบหน้าจอ (page margin) บน desktop |
| `--space-16` | 64px | ระยะห่างบล็อกใหญ่ใน Dashboard ผู้บริหาร |

**Grid & breakpoint:** ระบบเป็น **Responsive เต็มรูปแบบ** รองรับทุกอุปกรณ์ตาม [spec 006](01-requirements/01-spec/20260816-006-rbac-and-security-nfr.md) หัวข้อ "เพิ่มเติม 20260820" (เปลี่ยนจากมติ Desktop-first เดิม เนื่องจากเจ้าหน้าที่ รพ.สต. เริ่มใช้แท็บเล็ต/มือถือในการทำงานภาคสนามจริง) — breakpoint และ grid ต่อช่วง:

| Breakpoint | ช่วงความกว้างจอ | Grid columns | Container width | Page margin |
|---|---|---|---|---|
| Mobile | 320–767px | 4 คอลัมน์ | เต็มความกว้างจอ | `--space-4` (16px) |
| Tablet | 768–1023px | 8 คอลัมน์ | เต็มความกว้างจอ | `--space-6` (24px) |
| Desktop | 1024px ขึ้นไป | 12 คอลัมน์ | สูงสุด 1440px | `--space-12` (48px) |

**หลักการปรับ layout ข้าม breakpoint:** ออกแบบแบบ **mobile-first ในเชิง CSS** (เขียน style พื้นฐานให้ mobile ก่อน แล้วขยาย/เพิ่ม column ด้วย `min-width` media query ขึ้นไปที่ tablet/desktop) แต่ **เนื้อหา/ฟีเจอร์ต้องครบเท่ากันทุกอุปกรณ์** ไม่ตัดฟังก์ชันออกบนจอเล็ก — content ที่เคยแสดงหลายคอลัมน์พร้อมกันบน desktop (เช่น ตารางยอดคงคลัง) ให้ปรับเป็น layout แนวตั้ง (stacked) บน mobile/tablet แทนการซ่อนข้อมูล (ดู §3.3, §3.5 สำหรับ component ที่ต้องปรับเฉพาะ)

**Radius & elevation:** สอดคล้องกับโหนดมุมโค้งในโลโก้

| Token | Value |
|---|---|
| `--radius-sm` | 6px (input, badge) |
| `--radius-md` | 10px (card, modal) |
| `--radius-full` | 999px (pill/status badge) |
| `--shadow-card` | `0 1px 3px rgba(46,43,38,0.08)` — เงาบางมาก ใช้แยก surface เท่านั้น ห้ามใช้เงาหนักแบบ material design |

---

## 3. UI Components & Patterns

> หลักทั่วไป: ทุก component ใช้สีจาก token ใน §2.1 เท่านั้น ไม่ผสมสีใหม่นอกชุดนี้

### 3.1 Buttons

| ประเภท | ลักษณะ | ใช้เมื่อ |
|---|---|---|
| Primary | พื้น `--color-primary-600`, ตัวอักษรขาว | action หลักของหน้าจอ 1 ปุ่มต่อหน้าจอเท่านั้น (เช่น "ยืนยันคำขอเบิก", "อนุมัติ") |
| Secondary | ขอบ `--color-border`, พื้นโปร่งใส, ตัวอักษร `--color-text-primary` | action รอง (เช่น "บันทึกร่าง", "ยกเลิก") |
| Danger | พื้น `--color-danger`, ตัวอักษรขาว | action ที่ทำลาย/ปฏิเสธ (เช่น "ปฏิเสธคำขอ", "ลบ") — ต้องมี confirm dialog เสมอ (ดู §4.3) |
| Text/Ghost | ไม่มีพื้น/ขอบ, ตัวอักษร `--color-primary-700` | action รองมากในตาราง (เช่น "ดูรายละเอียด") |

ปุ่มทุกแบบสูงขั้นต่ำ 40px (คลิกง่ายสำหรับผู้ใช้ที่ไม่ถนัดเมาส์) มุมโค้ง `--radius-sm`

**Responsive:** บน mobile (< 768px) ปุ่ม Primary/Danger ที่เป็น action หลักของหน้าจอ **ขยายเต็มความกว้าง (full-width)** เพื่อให้แตะง่ายด้วยนิ้วมือ — ปุ่ม Secondary/Text ยังคงขนาดตามเนื้อหาปกติ ความสูงขั้นต่ำ 40px คงเดิมทุก breakpoint (จุดสัมผัสต้องไม่เล็กลงบนจอสัมผัส)

### 3.2 Form Inputs

- Input เดี่ยวสูง 40px, padding `--space-3`, ขอบ `--color-border`, โฟกัสแล้วขอบเปลี่ยนเป็น `--color-primary-600` หนา 2px (ไม่ใช้แค่เปลี่ยนสีเงา เพื่อ accessibility)
- ฟิลด์ตัวเลข (เช่น จำนวนเบิก, ยอดคงเหลือที่แจ้งเอง) ชิดขวา (right-align) ตามธรรมเนียมตัวเลขทางบัญชี/คลัง
- แสดง validation error เป็นข้อความสีข้อความปกติ + ไอคอนเตือนสี `--color-danger` ใต้ฟิลด์ทันที ไม่ใช้ popup/alert แยก
- Label อยู่เหนือ input เสมอ (ไม่ใช้ placeholder แทน label) — ป้องกันผู้ใช้ลืมว่าฟิลด์คืออะไรเมื่อพิมพ์ไปแล้ว
- **Responsive:** ฟอร์มที่จัดเป็นหลายคอลัมน์บน desktop (เช่น 2 ฟิลด์ต่อแถว) ให้ยุบเหลือ **1 คอลัมน์เต็มความกว้าง** บน mobile/tablet เสมอ — ไม่ลดขนาดตัวอักษร/ความสูง input ลงต่ำกว่าค่าที่กำหนดไว้

### 3.3 Data Table

ตารางเป็น component หลักของระบบ (ยอดคงคลัง, รายการคำขอเบิก, ประวัติรับ-จ่าย) ตาม [spec 002](01-requirements/01-spec/20260816-002-ncd-drug-requisition-core.md):

- แถวสลับสี (zebra) ด้วย `--color-bg-muted` เพื่อช่วยกวาดสายตาแนวนอนในตารางยาว
- คอลัมน์ตัวเลข right-align, คอลัมน์สถานะใช้ Status Badge (§3.4) แทนข้อความล้วน
- Sticky header เมื่อ scroll ตารางยาว (จำเป็นเพราะรายการยามีจำนวนมาก)
- แถวที่มีค่าต่ำกว่า safety stock หรือรอการดำเนินการนานผิดปกติ ให้เน้นด้วย **แถบสีบางที่ขอบซ้ายของแถว** (ไม่ใช่เปลี่ยนสีพื้นทั้งแถว) เพื่อคงความ minimalist แต่ยังสังเกตเห็นได้ทันที

**Responsive (Mobile/Tablet, < 768px):** ห้ามใช้ horizontal scroll เป็นทางออกหลัก เพราะผู้ใช้จำนวนมากไม่คุ้นเคยกับการเลื่อนแนวนอนในตาราง — ให้แปลงแต่ละแถวเป็น **card แนวตั้ง** ตามสไตล์ §3.6 แทน (label ชิดซ้าย : value ชิดขวาในบรรทัดเดียวกัน, คอลัมน์สถานะยังคงเป็น Status Badge, แถบสีเตือนขอบซ้ายยังคงอยู่ที่ card) เรียงเป็น list แนวตั้งแทนตาราง — เนื้อหา/ฟิลด์ต้องครบเท่าตารางเดิม ไม่ตัดคอลัมน์ทิ้ง

### 3.4 Status Badge

ใช้แสดงสถานะคำขอเบิกตาม flow ใน [spec 002](01-requirements/01-spec/20260816-002-ncd-drug-requisition-core.md) (ร่าง → รออนุมัติระดับ 1 → รออนุมัติระดับ 2 → พร้อมส่งออก → จ่ายแล้ว → รับแล้ว):

- รูปทรง pill (`--radius-full`), พื้น subtle (เช่น `--color-primary-100`), ตัวอักษรสีเข้มคู่กัน — ไม่ใช้พื้นสีทึบเข้ม
- 1 สถานะ = 1 สี คงที่ตลอดทั้งระบบ ห้ามใช้สีเดียวกันซ้ำกับสถานะอื่น
- สถานะที่ต้องการความสนใจสูง (เบิกฉุกเฉิน, ยาขาด) เพิ่มไอคอนเตือนเล็กๆ ในตัว badge ไม่ใช้ badge กระพริบ/เคลื่อนไหว

### 3.5 Navigation

**Desktop (1024px+):**
- **Sidebar ซ้ายคงที่** แสดงเมนูตาม role ที่ล็อกอิน (RBAC — ดู [spec 006](01-requirements/01-spec/20260816-006-rbac-and-security-nfr.md)) — เจ้าหน้าที่ รพ.สต. เห็นเฉพาะเมนูของหน่วยตนเอง, เภสัชกรเห็นเมนูอนุมัติ/คัดกรองทั้งเครือข่าย, ผู้บริหารเห็นเฉพาะเมนู Dashboard/รายงาน (read-only)
- Sidebar พื้น `--color-bg-muted`, เมนูที่เลือกอยู่ใช้พื้น `--color-primary-100` + ตัวอักษร `--color-primary-700`
- แถบบนสุด (top bar) แสดงชื่อหน่วยงาน/บทบาทผู้ใช้ปัจจุบันเสมอ — สำคัญเพราะเภสัชกรอาจสลับบทบาทอนุมัติระดับ 1/2 ได้ตามเวร ต้องชัดเจนว่ากำลังทำหน้าที่ไหนอยู่

**Tablet/Mobile (< 1024px):** Sidebar ซ้ายที่ตรึงอยู่ตลอดไม่เหมาะกับจอแคบ (กินพื้นที่เนื้อหาหลักมากเกินไป) — เปลี่ยนเป็น:
- **Top bar คงที่** แสดงไอคอนเมนู (hamburger) ซ้ายสุด + ชื่อหน่วยงาน/บทบาทผู้ใช้ปัจจุบัน (คงไว้เหมือน desktop เพราะสำคัญต่อการสลับบทบาทอนุมัติ)
- แตะไอคอนเมนูแล้วเปิด **Drawer overlay** เลื่อนจากซ้าย ใช้เมนู/สีเดียวกับ sidebar desktop ทุกประการ (พื้น `--color-bg-muted`, เมนูเลือกอยู่ `--color-primary-100`) เพื่อคงความ "เป็นเครือข่ายเดียวกัน" (§1.2) — ปิด drawer เมื่อแตะนอกพื้นที่หรือเลือกเมนูแล้ว
- ไม่ใช้ bottom tab bar เพราะเมนูมีจำนวนมากและต่างกันตาม role — hamburger + drawer เดียวพอสำหรับทุก role

### 3.6 Cards (Dashboard/KPI)

- ใช้ใน [spec 004](01-requirements/01-spec/20260816-004-management-dashboard.md) — 1 card = 1 ตัวชี้วัดหรือกราฟเดียว
- พื้น `--color-bg-surface`, เงา `--shadow-card`, มุมโค้ง `--radius-md`
- ตัวเลข KPI หลักใช้ `--font-size-display` หรือ `--font-size-h1` เพื่อให้ผู้บริหารอ่านจากระยะไกลได้ (ใช้ในห้องประชุม/นำเสนอ)
- กราฟใช้สีจาก semantic palette เท่านั้น ไม่เพิ่มสีใหม่ในกราฟ
- **Responsive:** จัดเรียงเป็น grid หลาย card ต่อแถวบน desktop (ตาม grid 12 คอลัมน์), ลดเหลือ 2 card ต่อแถวบน tablet, และ **1 card ต่อแถว (stacked เต็มความกว้าง)** บน mobile — ไม่ลดขนาดตัวเลข KPI หลักต่ำกว่า `--font-size-h1` แม้บนจอเล็ก เพื่อให้ยังอ่านง่าย

### 3.7 Modal / Confirm Dialog

- ใช้กับทุก action ที่ทำลาย/เปลี่ยนสถานะสำคัญ (อนุมัติ/ปฏิเสธ/ยืนยันรับยา) ตามหลัก confirm-before-critical-action ใน §4.3
- โครงสร้าง: หัวข้อสั้น (คำถามยืนยัน), รายละเอียดสรุปสิ่งที่กำลังจะเกิดขึ้น, ปุ่ม Secondary (ยกเลิก) อยู่ซ้าย + ปุ่ม Primary/Danger (ยืนยัน) อยู่ขวาสุด

### 3.8 Alert / Notification Banner

- ใช้กับการแจ้งเตือนภายในแอป (เช่น "ใกล้ถึงรอบเบิก") — แยกจาก LINE OA alert ภายนอกที่ระบุใน [spec 001](01-requirements/01-spec/20260816-001-project-scope-and-problem-background.md) (ฟังก์ชัน "ขอปรึกษา")
- แถบบางที่มุมบนของหน้าจอหรือใน context ที่เกี่ยวข้อง (ไม่ใช่ popup กลางจอที่บล็อกการทำงาน) ยกเว้นกรณี critical จริงๆ

---

## 4. UX Guidelines & Rules

### 4.1 หลักการทั่วไป

- **ข้อมูลต้องมาก่อน UI เสมอ** — จอที่ใช้งานบ่อย (รายการเบิก, ยอดคงคลัง) ให้ความสำคัญกับความหนาแน่นของข้อมูลที่อ่านง่าย มากกว่าความสวยงามเชิงตกแต่ง (สอดคล้องหลัก muji: "no more, no less")
- **1 หน้าจอ 1 action หลัก** — ปุ่ม Primary มีได้สูงสุด 1 ปุ่มต่อหน้าจอ/การ์ด เพื่อไม่ให้ผู้ใช้ลังเลว่าต้องกดอะไรก่อน
- **สม่ำเสมอทุก role** — แม้สิทธิ์การมองเห็นข้อมูลต่างกันตาม RBAC แต่ layout/สี/ศัพท์ที่ใช้ต้องเป็นชุดเดียวกันทั้งระบบ ไม่ออกแบบแยกกันคนละแนวต่อ role

### 4.2 Data Density & Responsive

- ระบบเป็น Web Application แบบ **Responsive เต็มรูปแบบ** ([spec 006](01-requirements/01-spec/20260816-006-rbac-and-security-nfr.md)) — บน desktop ออกแบบตารางและฟอร์มให้ใช้พื้นที่จอกว้างอย่างมีประสิทธิภาพ (เช่น แสดงหลายคอลัมน์พร้อมกันแทนการซ่อนใน accordion), บน mobile/tablet ให้จัดเรียงเนื้อหาเดียวกันเป็นแนวตั้ง (stacked) แทนการซ่อนหรือตัดข้อมูลออก (ดู §3.3 ตาราง, §3.6 card)
- หลีกเลี่ยงการซ่อนข้อมูลสำคัญไว้หลัง hover/tooltip เพียงอย่างเดียว — ผู้ใช้จำนวนมากไม่คุ้นเคยกับ interaction แบบนี้ (สำคัญยิ่งขึ้นบน mobile/tablet ที่ไม่มี hover)

### 4.3 Confirm-before-critical-action

การกระทำที่กระทบสถานะคำขอเบิก/คลังยาต้องมี confirm dialog เสมอ (ดู §3.7) ครอบคลุมอย่างน้อย:

- อนุมัติ/ปฏิเสธคำขอเบิก (ทั้งระดับ 1 และ 2)
- ยืนยันการรับยา/จ่ายยา
- แก้ไขข้อมูลย้อนหลังโดย Admin (กรณีฉุกเฉินตาม [spec 006](01-requirements/01-spec/20260816-006-rbac-and-security-nfr.md)) — dialog ต้องระบุชัดว่าการแก้ไขนี้จะถูกบันทึก audit log

**กรณีพิเศษ RBAC:** UI ต้อง**ซ่อนหรือ disable ปุ่มอนุมัติระดับ 2** โดยอัตโนมัติหากผู้ใช้ที่ล็อกอินอยู่เป็นคนเดียวกับที่อนุมัติระดับ 1 ของคำขอนั้นไปแล้ว (กฎห้ามคนเดียวกันอนุมัติซ้ำสองระดับ ตาม [spec 001](01-requirements/01-spec/20260816-001-project-scope-and-problem-background.md)) — ป้องกันที่ระดับ UI ไม่ใช่พึ่ง backend validation อย่างเดียว เพื่อลด friction ของผู้ใช้ (ไม่ต้องกดแล้วเจอ error ทีหลัง)

### 4.4 โทนภาษาและข้อความในระบบ (Content Tone)

- ใช้ภาษาที่ตรงไปตรงมา ไม่ใช้ศัพท์เทคนิคด้าน IT (เช่น หลีกเลี่ยงคำว่า "sync failed" ให้ใช้ "บันทึกข้อมูลไม่สำเร็จ ลองใหม่อีกครั้ง")
- ข้อความแจ้งเตือน/error ต้องบอกสิ่งที่ผู้ใช้ทำได้ต่อ ไม่ใช่แค่บอกว่าผิดพลาด (เช่น "ยอดคงเหลือที่กรอกมากกว่ายอดคงคลังปัจจุบัน กรุณาตรวจสอบอีกครั้ง" ไม่ใช่แค่ "ค่าไม่ถูกต้อง")
- หลีกเลี่ยงถ้อยคำที่ตำหนิผู้ใช้ (เช่น ไม่ใช้ "คุณกรอกผิด" ให้ใช้ "ข้อมูลนี้ต้องตรวจสอบอีกครั้ง")

### 4.5 Accessibility

- ทุกคู่สีตัวอักษร/พื้นหลังต้องผ่าน WCAG AA (ดู §2.1)
- ขนาดตัวอักษรพื้นฐานไม่ต่ำกว่า 16px สำหรับเนื้อหาที่ต้องอ่านเพื่อการตัดสินใจ (ตัวเลขคงคลัง, ยอดเบิก) — รองรับผู้ใช้ที่อาจมีปัญหาสายตาจากการทำงานหน้าจอนาน
- ทุก action สำคัญต้องกดได้ด้วยคีย์บอร์ด ไม่พึ่งเมาส์อย่างเดียว (เผื่อกรณีฮาร์ดแวร์เก่าที่บาง รพ.สต. อาจใช้งานอยู่)
- ห้ามสื่อความหมายด้วย "สีเพียงอย่างเดียว" — สถานะ/คำเตือนทุกจุดต้องมีข้อความหรือไอคอนกำกับคู่กับสีเสมอ (รองรับผู้ใช้ตาบอดสี)

### 4.6 ขอบเขตที่ยังไม่ครอบคลุมในเฟสนี้ (Open Items)

- **Dark mode:** ยังไม่ออกแบบ เนื่องจากไม่อยู่ในขอบเขตปัจจุบันของโครงการ — โทเคนสีในเอกสารนี้ยังไม่มีชุดคู่สำหรับพื้นเข้ม
- **โลโก้ vector สมบูรณ์:** รอส่งต่อให้นักออกแบบกราฟิกแปลงจากภาพร่างเป็น production-ready asset (ดู §1.3)
- **การยืนยันค่าสี/ฟอนต์ขั้นสุดท้ายกับผู้มีอำนาจตัดสินใจด้าน branding ของ รพ.เชียงรายประชานุเคราะห์** (เช่น ต้องใช้ตราสัญลักษณ์ราชการ/กระทรวงสาธารณสุขประกอบหรือไม่) — เอกสารนี้ยังไม่ได้ยืนยันจุดนี้

---

## เอกสารที่เกี่ยวข้อง (Related Documents)

- [CLAUDE.md](CLAUDE.md) — บริบทโครงการและ convention เอกสารโดยรวม
- [01-requirements/01-spec/20260816-001-project-scope-and-problem-background.md](01-requirements/01-spec/20260816-001-project-scope-and-problem-background.md) — ที่มาปัญหา, roles, glossary
- [01-requirements/01-spec/20260816-002-ncd-drug-requisition-core.md](01-requirements/01-spec/20260816-002-ncd-drug-requisition-core.md) — flow สถานะคำขอเบิกที่ผูกกับ Status Badge (§3.4)
- [01-requirements/01-spec/20260816-004-management-dashboard.md](01-requirements/01-spec/20260816-004-management-dashboard.md) — เนื้อหา Dashboard ที่ผูกกับ Cards (§3.6)
- [01-requirements/01-spec/20260816-006-rbac-and-security-nfr.md](01-requirements/01-spec/20260816-006-rbac-and-security-nfr.md) — RBAC ที่ผูกกับ Navigation (§3.5) และ Confirm-before-critical-action (§4.3)
