# Test Cases — FT-030 เข้ารหัสข้อมูลระหว่างส่งผ่านเครือข่าย (Data Encryption in Transit)

> อ้างอิงจาก [Feature List](../../02-feature-list.md), [Acceptance Criteria](../acceptance-criteria.md)
>
> หมายเหตุ: ข้อมูลทดสอบเป็น placeholder ทั่วไป ไม่ใช่ข้อมูลยา NCD จริง — ดู [test-plan.md](../test-plan.md) §4 — เป็น NFR เชิงระบบล้วนๆ ไม่มีหน้าจอที่ผู้ใช้ลงมือทำ ต้องมีการตั้งค่า HTTPS/TLS ในสภาพแวดล้อมทดสอบก่อน

| Test ID | Test Case Name | Pre-condition | Test Steps | Expected Result | Test Data | อ้างอิง |
|---|---|---|---|---|---|---|
| TC-125 | ข้อมูลเข้ารหัสระหว่างส่งเสมอ | เปิดใช้งานระบบผ่านเครือข่าย | 1. เข้าสู่ระบบ (login) <br> 2. บันทึกคำขอเบิก/ดาวน์โหลดไฟล์ <br> 3. ตรวจสอบการเชื่อมต่อ (เช่น protocol/certificate ของการเชื่อมต่อ) ระหว่างขั้นตอนดังกล่าว | ข้อมูลถูกเข้ารหัสระหว่างการส่งผ่านเสมอ ไม่มีช่องทางรับส่งข้อมูลจริงแบบไม่เข้ารหัส | บัญชีทดสอบ, คำขอเบิกตัวอย่าง | BL-039, AC Scenario 1, FR-6.9 |
| TC-126 | พยายามเข้าถึงผ่านช่องทางไม่เข้ารหัส | มีช่องทางเชื่อมต่อแบบไม่เข้ารหัสให้ทดสอบ (เช่น URL แบบไม่เข้ารหัส) | 1. พยายามเชื่อมต่อกับระบบผ่านช่องทางที่ไม่เข้ารหัส | **[ถือว่า]** ระบบปฏิเสธ/เปลี่ยนเส้นทางไปยังช่องทางที่เข้ารหัสเสมอ ไม่อนุญาตให้รับส่งข้อมูลจริงผ่านช่องทางที่ไม่เข้ารหัส | URL ทดสอบแบบไม่เข้ารหัส | BL-039, AC Scenario 2 (`[ถือว่า]`), FR-6.9 |

---

## บันทึกการอัปเดต (Changelog)

- **20260824 (test-design-writer, audit เต็มรูปแบบตามคำขอ `/check-backlog-sync`, หลังเพิ่ม BL-039/FT-030):** สร้าง Test Case ของ FT-030 ครั้งแรก — 2 test case (TC-125–TC-126) ครอบคลุมทั้ง 2 scenario ของ BL-039 ใน [acceptance-criteria.md](../acceptance-criteria.md) — TC-ID ต่อเนื่องจาก TC-124 (pharmacist-two-factor-authentication.md)
