# Test Cases — FT-034 Admin ปรับค่า Config ผ่านหน้าจอระบบ

> อ้างอิงจาก [Feature List](../../02-feature-list.md), [Acceptance Criteria](../acceptance-criteria.md), และ [admin-journey.md](../../03-user-journey/admin-journey.md)
>
> หมายเหตุ: ข้อมูลทดสอบเป็น placeholder ทั่วไป ไม่ใช่ข้อมูลยา NCD จริง — ดู [test-plan.md](../test-plan.md) §4 — cross-ref BL-009b: ยืนยันแล้ว 20260825 ว่าค่าเริ่มต้นจำนวนวันแจ้งเตือนล่วงหน้า = 2 วัน, ขอบเขต min/max ของ Config Key นี้ = 1 ถึง 30 วัน, และหลักการ validation กำหนดเฉพาะทีละ Config Key (ไม่ใช้กฎเดียวกันทั้งหมด) — Config Key อื่นที่จะเพิ่มในอนาคตยังต้องยืนยันขอบเขต min/max เป็นรายตัวเมื่อเพิ่มจริง

| Test ID | Test Case Name | Pre-condition | Test Steps | Expected Result | Test Data | อ้างอิง |
|---|---|---|---|---|---|---|
| TC-134 | Admin ปรับค่า Config ผ่านหน้าจอสำเร็จ | Login ด้วยบัญชี Admin | 1. เข้าหน้าจอตั้งค่าระบบ <br> 2. ปรับค่า Config Key "จำนวนวันแจ้งเตือนล่วงหน้า" (BL-009b) จากค่าเดิม 2 วัน เป็น 3 วัน <br> 3. กดบันทึก | ค่าใหม่ (3 วัน) มีผลทันทีกับรอบแจ้งเตือนถัดไปของ FT-007 โดยไม่ต้องแก้ไขโค้ด/รอรอบพัฒนาใหม่ | บัญชี Admin, Config Key "จำนวนวันแจ้งเตือนล่วงหน้า" เปลี่ยนจาก 2 วัน เป็น 3 วัน | BL-043, AC Scenario 1, FR-6.10 |
| TC-135 | กรอกค่า Config นอกขอบเขตที่ยอมรับได้ของ Config Key นั้น | Login ด้วยบัญชี Admin และอยู่ในหน้าจอตั้งค่าระบบ | 1. กรอกค่า 0 ให้ Config Key "จำนวนวันแจ้งเตือนล่วงหน้า" (ขอบเขตที่ยอมรับได้ = 1 ถึง 30) <br> 2. กดบันทึก <br> 3. แก้เป็น 31 แล้วกดบันทึกอีกครั้ง | ทั้งขั้นตอน 2 และ 3: ระบบปฏิเสธการบันทึกและแจ้งข้อความว่าต้องกรอกค่าระหว่าง 1 ถึง 30 — ค่าเดิม (2 วัน) ยังคงไม่เปลี่ยนแปลง | Config Key "จำนวนวันแจ้งเตือนล่วงหน้า" = 0 แล้ว = 31 (ขอบเขตที่ถูกต้อง: 1-30) | BL-043, AC Scenario 2, FR-6.10 |
| TC-141 | Config Key หนึ่งผิดพลาดไม่กระทบ Config Key อื่น | Login ด้วยบัญชี Admin และอยู่ในหน้าจอตั้งค่าระบบ ซึ่งมี Config Key อื่นตั้งค่าไว้ก่อนหน้าแล้ว | 1. พยายามบันทึกค่าที่ไม่ผ่าน validation ของ Config Key "จำนวนวันแจ้งเตือนล่วงหน้า" (ตาม TC-135) <br> 2. ตรวจสอบค่าปัจจุบันของ Config Key อื่นทั้งหมด | ค่าปัจจุบันของ Config Key อื่นทั้งหมดยังคงเดิมไม่เปลี่ยนแปลง (การ validation ทำงานแยกเป็นรายรายการ) | Config Key อื่น (placeholder) ที่มีค่าตั้งไว้ก่อนหน้า | BL-043, AC Scenario 3, FR-6.10 |
| TC-136 | Role อื่นพยายามเข้าถึงหน้าจอตั้งค่า | Login ด้วยบัญชี role อื่น (ไม่ใช่ Admin) | 1. พยายามเข้าถึงเมนู/URL หน้าจอตั้งค่า config ของระบบ | ระบบปฏิเสธ/ไม่แสดงเมนูนี้ (สิทธิ์ปรับ config เป็นของ Admin เท่านั้น) | บัญชีเจ้าหน้าที่ รพ.สต./เภสัชกร/ผู้บริหาร | BL-043, AC Scenario 4, NFR Security |

---

## บันทึกการอัปเดต (Changelog)

- **20260824 (test-design-writer, audit เต็มรูปแบบตามคำขอ `/check-backlog-sync`, หลังเพิ่ม BL-043/FT-034):** สร้าง Test Case ของ FT-034 ครั้งแรก — 3 test case (TC-134–TC-136) ครอบคลุมทั้ง 3 scenario ของ BL-043 ใน [acceptance-criteria.md](../acceptance-criteria.md) — TC-ID ต่อเนื่องจาก TC-133 (disaster-recovery.md), เป็น TC-ID สูงสุดในชุด test-cases/ ทั้งหมด ณ ปัจจุบัน (TC-136)
- **20260825 (test-design-writer, audit เต็มรูปแบบตามคำขอผู้ใช้ หลังปิด `[รอยืนยัน]` ของ BL-009b/BL-043):** แก้ **TC-134** ให้ใช้ตัวอย่างที่เป็นรูปธรรม (ปรับ Config Key "จำนวนวันแจ้งเตือนล่วงหน้า" จาก 2 เป็น 3 วัน) แทน placeholder "7 วัน" เดิม แก้ **TC-135** ให้สะท้อนหลักการ "ขอบเขต min/max แยกทีละ Config Key" ที่ยืนยันแล้ว (คง `[ถือว่า]` เฉพาะตัวเลข min/max ที่แน่นอนซึ่งยังไม่ได้ระบุ) เพิ่ม **TC-141** ใหม่ (edge — isolation, Config Key หนึ่งผิดพลาดไม่กระทบ Key อื่น) ตรงกับ Scenario 3 ใหม่ใน [acceptance-criteria.md](../acceptance-criteria.md) ต่อเนื่องจาก TC-140 (pharmacist-two-factor-authentication.md) แก้ **TC-136** อ้างอิงเป็น "AC Scenario 4" ตามลำดับ scenario ใหม่ (ไม่เปลี่ยนเนื้อหา test case) — **TC-141 เป็น TC-ID สูงสุดในชุด test-cases/ ทั้งหมด ณ ปัจจุบัน**
- **20260825 (ยืนยันรอบ 3, ปิด `[ถือว่า]` ของ TC-135):** ผู้ใช้ยืนยันผ่าน `AskUserQuestion` ว่าขอบเขต min/max ของ Config Key "จำนวนวันแจ้งเตือนล่วงหน้า" (BL-009b) = 1 ถึง 30 วัน — เขียน **TC-135** ใหม่ให้ทดสอบทั้งขอบล่าง (0) และขอบบน (31) แทน `[ถือว่า]` เดิมที่มีแค่ตัวอย่างค่าติดลบ
